import QtQuick

// 设置页。**刻意留白。**
//
// 现在还不到做设置的时候：用户设置文件本身还没定（是 ini 还是 json、放哪儿、
// 哪些项要持久化），先画一堆控件上去，等设置文件定了还得全改一遍。
//
// 所以这里就是一个空的 Item —— 它的作用是**证明顶栏的切换是真的能换页**，
// 而不是留一个占位符。填内容的那天，TopNav 和窗口那边一行都不用动。
Item {
}
