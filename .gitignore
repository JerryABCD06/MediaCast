# ── 构建产物 ─────────────────────────────────────────────────────────────
#
# Qt Creator 的构建目录和命令行 CMake 的输出都在这里。它们随时可以重新生成，
# 而且里面有几 GB 的中间文件（PDB、ILK、obj），不进版本控制。
build/

# ── Qt Creator 的工程私有设置 ────────────────────────────────────────────
#
# .user 里存的是"这台机器上这个工程的构建设置"——套件、构建目录、并行数。
# 换台机器就失效了，不该跟着代码走。
*.user
*.user.*

# ── 第三方源码：FluentUI ─────────────────────────────────────────────────
#
# 它是一个独立的 git clone（自己带 .git），所以不能纳入我们的版本控制 ——
# 否则会变成"仓库里套仓库"，git 只会记成一个空的目录项，看着像丢了东西。
#
# 代价：clone 出来的那份代码不在我们的历史里。所以：
#   · 用的是哪个提交，记在 third_party/PATCHES.md 里
#   · 我们改过它的哪几行，也记在那儿
# 换机器时照那份文档重新 clone + 打补丁即可。
third_party/FluentUI/

# ── 编辑器/系统的杂物 ────────────────────────────────────────────────────
#
# `.vs/` 是 Visual Studio 的**本机缓存**：浏览数据库、CMake 缓存、符号索引……
# 里面存着这台机器的绝对路径，换台机器全失效。之前漏了这条，结果它有十个文件
# 被跟踪进了仓库 —— 已经用 `git rm -r --cached .vs` 清掉跟踪，这儿补上规矩。
.vs/

*.swp
*~ 
Thumbs.db
desktop.ini
