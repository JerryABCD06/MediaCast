# ── 构建产物 ─────────────────────────────────────────────────────────────
#
# Qt Creator 的构建目录和命令行 CMake 的输出都在这里。它们随时可以重新生成，
# 而且里面有几 GB 的中间文件（PDB、ILK、obj），不进版本控制。
build/

# ── Qt Creator 的工程私有设置 ────────────────────────────────────────────
#
# .user 里存的是"这台机器上这个工程的构建设置"——套件、构建目录、并行数。
# 换台机器就失效了，不该跟着代码走。
*.user
*.user.*

# ── 第三方源码：FluentUI ─────────────────────────────────────────────────
#
# **它现在在版本控制里**（2026-09-14 改的）：整份 628 个文件都带进来了。
# 好处是"我们改了库的哪几行"在 git 里就是一个看得见的 diff，换台机器 clone
# 下来直接能编，不用先照文档去 clone 上游、再逐条打补丁。
#
# 但**它自己那份上游 `.git/` 不能进去** —— 那会变成"仓库里套仓库"，git 只记成
# 一个空的目录项。所以把它改名成了 `.git-upstream/`（留在盘上，方便和上游比、
# 将来升级时用），只忽略这一个目录。
#
# 上游是哪个提交、我们打了哪些补丁：`third_party/PATCHES.md`。
third_party/FluentUI/.git-upstream/

# ── 编辑器/系统的杂物 ────────────────────────────────────────────────────
#
# `.vs/` 是 Visual Studio 的**本机缓存**：浏览数据库、CMake 缓存、符号索引……
# 里面存着这台机器的绝对路径，换台机器全失效。之前漏了这条，结果它有十个文件
# 被跟踪进了仓库 —— 已经用 `git rm -r --cached .vs` 清掉跟踪，这儿补上规矩。
.vs/

*.swp
*~ 
Thumbs.db
desktop.ini
