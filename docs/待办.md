# 待办

> 这份文件回答两个问题：**现在到哪儿了**、**下一步干什么**。
> DLNA 协议本身的实现情况在 `DLNA实现情况.md`，语言文件的格式和规矩在
> `lang/README.md`，**真实控制点实际怎么做**记在 `控制点行为.md` —— 这三样
> 这儿都不重复。
>
> 只想快速接上手的看 `状态速览.md`（一屏，环境 / 路径 / 分层 / 硬约束）。

最后更新：2026-09-13

---

## 现在到哪儿了

代码全在版本控制里。**具体的提交用 `git log --oneline` 看** —— 这份文件里
不抄提交号，抄了第二天就过时。

**能跑的**：手机通过 DLNA 投屏、电脑上播放；托盘常驻；勿扰模式；Windows 媒体
面板；新界面（FluentUI）的画面、顶栏、设置页、控制栏、悬停提示；语言能切
（界面和托盘一起变）。

**最近这几轮做完的**：

- **控制栏的显隐规则**（`components/MediaBar.qml` 顶部那段注释是权威）：
  空闲 / 放音乐**常显**；视频**在放**时鼠标进下沿那条热区就显示、挪开立刻收；
  视频暂停 / 放图片算"静止画面"——鼠标在整个画面框里动一下就显示，停手或挪到
  窗口外 3 秒后收；**刚投上来的视频**先露 5 秒（那 5 秒里不管鼠标在哪），
  图片没有这 5 秒。两条兜底：鼠标停在栏自己身上时不收；播放↔暂停切换时规则直接换。
- **全屏**：媒体栏右端那个键 + **双击画面** + **Esc** 退出。全屏 = 无边框铺满
  （标题栏隐去、内容区吃掉顶栏那 48 像素）；退出**原样返回**——普通窗口回普通、
  最大化回最大化，位置尺寸都不变。一个事实 + 一张表 + 一处执行的写法见
  `NewUiWindow.qml` 里那段。
- **云母底改成自己画**（`components/MicaBackdrop.qml`）。以前的分工是"窗口透明、
  页面跟着透"，于是每个要挡住下层内容的页面都得配一份"我底下压着什么、它要不要
  收起来"的知识 —— 加一页就多想一层。现在反过来：**谁要底谁摆一块实心的**，
  关掉开关就是那块 #F3F3F3；系统云母那行 `effect: "mica"` 还留着，但被盖住了。

  **分两步：造一次，各处只裁。** 造的那一层全窗口只有一处（窗口背景，`isSource`
  —— 它不在被裁剪的内容区里，这一点很要紧）；它把**整块屏幕那么大**的云母
  （壁纸模糊 + 染色 + 噪点）算好、钉在屏幕坐标系上。别处的底一律从这张图上裁
  自己那一格（`ShaderEffectSource` 取像素），**不重算**。

  为什么非要这样：一开始是"每块底各算各的"，接缝上会偶尔冒出一道淡淡的横线 ——
  模糊里的噪点层是按各自左上角平铺的，两块算出来差两三个色阶。而且窗口一动、
  页一换就得重算。现在**所有底都来自同一张图的同一批像素**，接缝在结构上不可能
  存在；窗口移动缩放只改"裁哪儿"，不动那张图。逐点比对过：设置页和投屏页在
  同一坐标上差 0。
- **状态机收尾**：片子自然播完、换下一条、加载期间按停止 —— 三种情况的状态
  都对得上了（前两个的坑在 keep-open，第三个是"加载好了"把停止顶回去）。
- **无文字的按钮都有悬停提示**：方向按位置挑 —— 顶栏和窗口三键往下弹，
  压在画面下沿的控制栏往上弹。
- **语言系统重做**：`lang/*.json` 明文、不编译，启动扫目录，代码里只剩键名。
  **到这一版为止全搬完了**（69 个键，含内容类型名和来源名）—— 托盘、新界面、
  Windows 媒体面板一个中文都不剩，加语言只要往目录里丢一个文件。
- **控制栏的标题/副标题接上了**：回退链（真标题 → 从文件名推 → 类型名）在
  C++ 里只此一份，界面拿到就显示；副标题是"歌手 → 专辑 → 未知"，主界面上
  **不显示"投送/本地播放"**（那是来源，不是内容）。
- **副标题按内容类型分工**：音频看**歌手**（音频里没有"副标题"这回事），
  视频和图片看**副标题**（DIDL 的 `upnp:subtitle` → `dc:description` →
  `upnp:longDescription`，过一遍占位符筛子）。视频图片**不拿艺术家顶** ——
  那儿的"艺术家"是演员/导演，含义完全不同。
- **文件自带的标签也读进来了**（mpv 的 `metadata`）：**只补协议没给的那几样**，
  所以标题的优先级是「协议 → 文件标签 → 从地址推 → 类型名」。
  实测很有用：投一个内部名字叫 `lrc` 的文件，界面先显示"lrc"（地址推的），
  两秒后文件打开、标签到位，标题自己更正成文件里写的 "TestSong"。
- **Windows 媒体面板不再暴露协议名**：副标题写成「来源 - 副标题」
  （"投送 - 周杰伦"），不再是 "DLNA 投送"。
- **关窗确认框的判据**改成"有没有投送方连着"，不再是"mpv 手里有没有东西"。

**播放层的形状**（没变）：

```
MediaPlayer（抽象接口，协议层只认它）
    └── MpvCore               mpv 实例 + 事件线程 + 状态缓存 + 命令 + 画面调节
          ├── LibMpvPlayer    wid 外壳：画面画进原生窗口（旧界面用）
          └── MpvQmlItem      render API 外壳：画面由 QML 场景图合成（新界面用）
```

**协议层已经解耦**：`DlnaRenderer` 不直接碰播放器，中间隔着
`PlaybackController`（中性枚举 `State` / `PlayMode` / `LoadStatus`，队列三格，
看门狗）。将来接别的协议时，新协议只要说中性的话就行。

---

## 一、等着做的

### 1. 新界面还没接的功能（现在都是壳子）

控制栏右边那四个键（`src/ui/qml/components/MediaBar.qml`）—— **全屏那个已经接好了**，
剩下三个：

- **音量**：**已完成（2026-09-14）**。点它在按钮正上方弹一条纵向滑块 —— 照
  Windows 11 那个音量浮出控件的样子（上面数字、下面滑块）。顺带给
  `PlaybackController` 补了 `volumePercent` / `muted` 两个 Q_PROPERTY
  （原来只有方法、没暴露给 QML）。

  **滑块是自己画的，不是 FluSlider**。理由在 `MediaBar.qml` 那一段注释里，
  一句话版：纵向 Slider 的**显示侧认 `from/to`、点击侧却是归一化的**，
  配不出"点上面给大值"。自己画之后显示和输入都由同一处算，方向不可能再反。
  规矩只有一条：**volume 0..100，顶 = 100**（和 Windows 那条一致）。

  **量过的**（不是"看着对"）：

  | 验的东西 | 方法 | 结果 |
  |---|---|---|
  | 点顶端 | 点滑块顶上的空白（轨道之上） | `GetVolume` = **100** |
  | 点底端 | 点滑块底下的空白（轨道之下） | `GetVolume` = **0** |
  | 点正中 | 点轨道中间 | `GetVolume` = **50** |
  | 手柄位置 | 扫中轴那一列像素，找手柄圆心 | 47 → 量到 808，算出来 807.6 |
  | 反方向 | 控制点发 `SetVolume(80)` | 手柄圆心量到 754.5，算出来 755 |

  **2026-09-14 又修了三处**（都是他看出来的，不是我看出来的）：

  | 毛病 | 原因 |
  |---|---|
  | 浮出控件只有左上角是圆的 | `FluRectangle.radius` 要四个角（见「别踩的坑」那条） |
  | 蓝色（已到）和灰色（没到）一样粗 | 规矩是**蓝 6、灰 4**：库自己的 FluSlider 就是灰的缩一圈、蓝的占满，上面那条进度条也照抄了它 |
  | 拖的时候不响，松手才变 | 原来抄了进度条"松手才写回"的规矩 —— 那是为**跳转**定的（每帧发 seek 会淹掉播放器）；音量只是设一个属性，改成一拖就发 |

  拖的时候变化密，曾经担心把控制点的 GENA 事件刷爆。**量过了，不用节流**：
  连着发 30 次 `SetVolume`（30 毫秒一条），订阅端只收到 **2 条** NOTIFY ——
  `GenaManager::sendEvent` 里本来就写着"同一个订阅同一时刻只允许一条在路上"，
  后面的变化合并成一条（那是为 SEQ 顺序定的，不是为省资源）。量它的脚本：
  `work\gena-volume-burst.ps1`。

  **按钮图标也跟着状态走了**：静音一个（`Mute`，带叉）、音量为 0 一个
  （`Volume0`，一个波都没有）、再按响度分三档（`Volume1`~`Volume3`，
  1-33 一道 / 34-66 两道 / 67-100 三道）。都是库里现成的字形。
  中间那三个键也换成**实心**的了（`PlaySolid` + `BackSolidBold` /
  `ForwardSolidBold`），和系统播放器一致。
  验法不用手点：从协议侧把音量挨个设一遍、每次抓图裁出那个按钮拼成一条 ——
  `work\vol-icon-states.ps1`。

  **静音怎么切**（2026-09-14 他定的规矩）：**面板开着的时候，点那个音量图标
  就是静音 / 取消静音，面板不关**；点面板以外任何地方才关面板（而且那一下被吃掉，
  不会漏到画面上把片子暂停）。

  做法值得记一笔，因为第一版走了弯路。**先试的是"给整窗模态遮罩挖个洞"**：把遮罩
  拆成上/下/左/右四块，围出按钮那一格。结果卡在"算出按钮在窗口里的绝对坐标"上 ——
  沿 parent 链累加和 `mapToItem` 给的是同一个值，可它跟屏幕上量出来的**整整差 48
  像素**（正好是顶栏高度），量了半天也没弄清那 48 从哪来。**他一句话点醒：在面板里
  摆一块透明按钮盖上去不就行了。**

  改成那样之后，坐标根本不用算：`Popup.x / .y` 本来就是**相对那个按钮**的，
  按钮在这一层里的位置就是它们的相反数。面板跟着按钮走，覆盖块也就跟着走 ——
  整类"窗口坐标"的坑一次性绕开了（那条弯路也别重走）。

  两条细节：

  - **上面那块自己不画图标**（`iconSource: 0`）。图标由下面那个真按钮画 ——
    同一位置叠两遍，抗锯齿的边缘会变深，那个键看着比旁边粗一圈。
  - 它是**控制栏那个按钮组件**（`TipIconButton`），所以悬停有底色、按下有动画；
    颜色必须引用**这条栏自己那套**（`bar.itemHoverColor` / `itemPressColor`），
    不能用 `FluTheme` 那套 —— 栏压在画面上时永远是深色，两套会对不上。

  探针脚本：`work\vol-test.ps1`（点三个落点、每步读回音量）、
  `work\probe-popup-slider.ps1`（扫中轴像素、分色段）、`work\crop.ps1`（裁图放大）。

  **坐标是量出来的，别猜**：窗口内物理像素（125% 缩放），滑块 Item 的上下端
  在 y 710..897、轨道在 722..884、音量按钮中心在 (1113, 948)。
- **字幕**：功能没做
- **显示效果**：**已完成（2026-09-14）**。控制栏那个太阳图标开的是一个**独立窗口**
  （不是弹层），十来条滑块一列摆开，顶部一句说明 + 「全部复位」。

  **界面上没有一项是写死的**：有几项、每项什么量程、复位到哪儿，全从
  `Playback.pictureControlList` 来（源头是 `MpvCore` 里那张照 mpv 选项表抄的表）。
  后端哪天多一项能调的，界面一行都不用改。

  **文案的落点也改了**（原来记在这儿的那件事）：`PictureControlInfo` 里的
  `label` 字段**删掉了** —— 中文名写死在 C++ 表里，界面切到英文照样蹦中文。
  现在后端只给中性短名（"brightness"），界面按 `ui_picture_<name>` 去语言文件里查
  （短名里的短横线换成下划线："pan-x" → `ui_picture_pan_x`）。旧界面那个下拉框
  现在显示的就是短名本身 —— 它是测试界面，不翻译，这是有意的。

  **三扇窗各自的底都自己造**：这扇窗也摆了一块 `MicaBackdrop`，而且**是自己
  `isSource: true` 造的那一张**，不跟主窗口共用。原因写在 `PictureWindow.qml` 里：
  `ShaderEffectSource` 的源 item 必须和它在**同一个窗口**，跨窗口指过去只会拿到
  一张空图 —— 而空图的表现正好是"窗口透过去"（这个坑当场踩过一次）。
  两边因为用的是同一张壁纸、同一个模糊半径和染色、又都钉在屏幕原点上，
  裁的是屏幕坐标里各自那一格，所以两扇窗叠在一起也不会出色差。

  **调过的值会记住**（2026-09-14 他定的）：存在 `MediaCast.json` 的 `picture`
  段里，键名就是后端那张表的短名（`picture.brightness` 之类）。

  规矩有两条，写在 `AppSettings` 和 `main()` 的注释里，别改错：

  - **权威在播放器手里，文件只是"备忘"。** 方向只有一条：播放器变了 → 写文件。
    反方向只在**开机那一次**读（`player->start()` 之后那几行 —— 早了 mpv 还没建
    起来，设属性是空操作）。所以不存在"两份状态谁说了算"。
  - **只存改过的项，文件里没有的不碰。** 让播放器留着自己的默认值，比我们猜一个
    "0"塞进去靠谱（以后加一项、或者某项的中性值不是 0 都不会出错）。

  拖滑块一次能发几十条变化，所以写盘是**攒 800 毫秒一起写**的（`AppSettings`
  里那个单次定时器）；拖完就退出的话，析构里会把没落盘的补上。
  控制点（手机）调的也走同一条路 —— 它也是"播放器的值变了"，所以手机上拉过的
  亮度同样会记住。

  验过：`SetBrightness(75)` / `SetContrast(20)` → 文件里出现 `brightness: 50`、
  `contrast: -60`（就是 DLNA 那条 0~100、50 为中性的换算）→ 重启 → 读回来仍是
  75 / 20，而没碰过的 Sharpness 还是 50。
- **全屏**：要处理 `FluFrameless` 那套边框助手

控制栏中间那两个键（上一首 / 下一首）：**已接（2026-09-14）**。

- 后端那边早就是齐的（`PlaybackController::next()` / `previous()`，还有
  `queueChanged(hasNext, hasPrevious, …)`），只是没暴露给 QML —— 现在加了
  `Q_INVOKABLE` 和两个 `Q_PROPERTY`（`hasNext` / `hasPrevious`）。
- **灰显用的是同一份判据**（那两个属性）：没内容就灰、点不动；亮着就一定点得动
  —— 不会出现"键亮着、按了没反应"。灰的颜色走**这条栏自己那套**
  （`disabledIconColor`），不能用库默认的 `FluTheme` 灰（那是按程序主题算的，
  压在暗画面上会糊）。
- **大多数时候这两个键是灰的，那是如实显示**：`hasPrevious` 是"刚才放过的那条"，
  `hasNext` 是控制点用 `SetNextAVTransportURI` 排进来的那条 —— 规范那一侧我们
  做对了，但实际控制点普遍不排下一条、也不按上一首，详见 `DLNA实现情况.md`。

  验法（不用手点）：协议侧读 `AVTransport#GetCurrentTransportActions` ——
  它就是拿 `hasNext/hasPrevious` 算出来的；再配上把中间三键裁出来的拼图看一眼。
  脚本：`work\queue-buttons-test.ps1`。走一遍"投第一条 → 排下一条 → 下一首 →
  上一首 → 清空"，动作清单依次是 `Pause,Stop,Seek` / `…,Next` / `…,Previous` /
  `…,Next` / `Pause,Stop,Seek`，界面上灰显跟着一一对上。

### 2. 旧界面退场

**做了一半**：托盘菜单的「打开主界面」开的是新界面；旧的改叫「打开测试界面」，
只当 DLNA 回归测试的观测窗口用。剩下：

- 启动时不再弹旧窗口（**现在还是会弹**）
- 删掉 `ui/MainWindow.*`、`ui/qml/TopNav.qml`、`main()` 里的 `kRenderVideoInNewUi`

**别提前删。** 旧界面现在还是 DLNA 回归测试的观测窗口，而且新界面的播放控制
还是壳子 —— 等上面第 2 条做完再动。

**那扇窗里已经有一处是"会撒谎的重复开关"**（2026-09-14 查出来的，还没动）：

- **「高频广播（10 秒一次，更容易被搜到）」那个复选框**。它调的是
  `m_renderer->setAliveIntervalMs(on ? 10000 : 60000)`。

  三件事凑在一起让它不该留着：

  一、**它从来不去读真实值** —— 构造时直接 `setChecked(true)`，所以哪怕设置里
  存的是 30000，它也显示"勾着"（意思是 10 秒）。
  二、**它的改动不落盘、也不通知设置页**：它绕过 `AppSettings` 直接设运行时的
  间隔，于是设置页那边的下拉框还显示老值，两处界面当场互相矛盾；下次重启又是
  文件里那个值。
  三、**它本来就是重复的**：新界面「投送 → 广播间隔」有 5 / 10 / 30 / 60 秒
  四档（外加"定期广播"总开关），把它的两档全包了。

  两条路：**删掉**（老界面迟早整扇退场，而回归测试要改间隔可以直接改
  `MediaCast.json` 再重启），或者**接到 `AppSettings` 上**（那就要给 MainWindow
  传 `AppSettings&`，为一件快删掉的东西加耦合）。倾向删。

> 一个 mpv 实例**只能有一条画面输出路径**（`vo` 只能设一次，wid 和 render API
> 互斥）。所以 `kRenderVideoInNewUi` 是过渡开关：为 true 时旧界面的画面区是
> 空的。旧界面一走，它和这个开关一起删。

### 3. 发布目录整理

**能打包了**，两条命令：

1. `work\configure-mcast-release.ps1 -Build` —— 配一个 Release 构建目录
   （`work\mcast-release`；Ninja 一个目录只能是一种构建类型，所以另开一个，
   不碰平时用的 Debug 目录）。
2. `work\deploy-mcast.ps1` —— 先在干净的暂存目录里摆好 exe + `libmpv-2.dll` +
   `lang/` + `qml/`，再让 windeployqt 往这个干净目录里补 Qt 的 DLL 和插件，
   最后整份拷到目标（默认桌面上的 `Media Cast Receiver`）。

包里的样子：`MCast.exe` + Qt 的 DLL 直接放程序根目录 + `platforms/`
`imageformats/` `iconengines/` `qml/` `styles/` `tls/` 这些插件目录 + `lang/` +
`libmpv-2.dll`。合计约 244 MB —— 其中 `libmpv-2.dll` 一份就 115 MB（ffmpeg
整个在里面）、`dxcompiler.dll` 24 MB、`opengl32sw.dll` 20 MB。

**有一处和最早定的不一样**：libmpv 没放进 `runtime/mpv/`，DLL 直接躺在 exe
旁边 —— 构建的 post-build 本来就这么拷，放到别处反而要额外加 DLL 搜索路径。
（`runtime/mpv/` 里那份 mpv.exe 是给备用的 `MpvMediaPlayer` 后端用的，不进包。）

**这些脚本有个坑**：里面是中文注释，而无 BOM 的 UTF-8 脚本用 Windows
PowerShell 5.1（`powershell -File`）跑会被按 ANSI 解码，注释能吃掉下一行代码。
在 pwsh 7 里直接 `& .\xxx.ps1` 跑就没事 —— 和以前那几个脚本一样。

**打包时最容易漏的一样东西**：windeployqt 的 `--qmldir` 得给**两处** ——
源码的 `src/ui/qml`，和暂存目录里的 `qml/`（FluentUI 那一坨）。只给前者的话，
FluentUI 里 `FluAcrylic.qml` 要的 `Qt5Compat.GraphicalEffects` 不会被带上，
而**开发机上一切正常**（Qt 安装目录里那份顶上了），打包出来一开新界面就
`module "Qt5Compat.GraphicalEffects" is not installed`，新界面干脆不出来。
现在 `deploy-mcast.ps1` 两处都给，而且部署完会把两处 QML 里 import 的模块名
全抠出来挨个在包里找一遍，缺哪个就报出来（`MediaCast` 是我们自己用 C++ 注册的，
不算，脚本里排除掉了）。

还差：安装包、快捷方式、版本号进「应用和功能」、把用不上的那几套 QML 样式
（Fusion / Imagine / Material / Universal）从包里剔掉（大概能省十几 MB）。

### 4. 发布前的法律/合规

`docs/legal/` 里是他那边出的一套声明（13 份，ChatGPT 帮忙起草的）。**框架没问题**
—— 不自称认证、区分"兼容"和"背书"、明确不提供内容、留了存疑空间。但那是**通用
模板**，缺的是针对本工程的具体内容。下面这些是照实际依赖补出来的（**工程视角的
常识梳理，不是法律意见**）：

**① `LICENSE` —— 已定：`GPL-3.0-or-later`。**
理由：链接了 libmpv 就**必须** GPL 兼容（硬约束）。在可选项里选 v3 而不是 v2+，
是因为 v3 有明确的专利条款（§11），而且**与 Apache-2.0 兼容**（v2 不兼容）——
而依赖里没有 GPLv2-only 的东西，所以"v2+ 才能合并老代码"这点好处我们用不上。

**已完成**：`LICENSE` 已放在**仓库根目录**，正文取自 gnu.org 的当前官方版
（35,149 字节、纯 LF、无 BOM，SHA256 开头 `3972DC97…`；与 GCC 树里那份逐行比对，
唯一差异是官方把 `http://` 更新成了 `https://`，正文同一份）。
`docs/legal/LICENSE-PLACEHOLDER.md` 已删（它说的"还没选"已不成立）。

**版权方**：`Wang Yunzheng <wyz-mcast@outlook.com>`（2026）。

**已完成**：57 个源文件都加上了 SPDX 两行头（`.cpp/.h/.qml/.rc` 用 `//`、
`CMakeLists.txt` 用 `#`、`.qrc` 用 `<!-- -->`；`lang/*.json` 跳过，JSON 没有注释
语法；`third_party/` 跳过，那不是我们的代码）。加头的脚本是
`work\add-spdx.ps1`（**保原编码、保原换行、幂等**，重复跑不会重复加）。
加完重编通过、也真跑过一次新界面（无 QML 报错）。`NOTICE.md` 和
`CONTRIBUTING.md` 同步补上了许可声明与"inbound = outbound、不签 CLA、不做版权
转让"的说明。

**已完成**：`licenses/`（许可全文 + 组件清单）建好并接进发布流程；
`NOTICE.md` / `CONTRIBUTING.md` / `SECURITY.md` 已挪到**仓库根目录**；
`docs/legal/` 里那几份按受众重新整理过（新增 `docs/legal/README.md` 当索引）。

**"休整"改了什么**（把模板腔改成事实）：

- `PRIVACY.md` 重写：明确"无遥测、不上报、不主动联网"，列出实际留下的三个文件
  （`MediaCast.json` / `MCast.log` / `%LOCALAPPDATA%\MCast\renderer.ini`）、
  实际监听的两个端口（8200 / 1900），并点明"日志里有计算机名和播过的媒体 URL"。
- `LEGAL-NOTICES.md` 第 5 节（网络）改成具体的：监听哪两个端口、**会把计算机名
  广播到局域网**、不受信任网络下该关哪个开关。
- `CODECS.md` 按性质分两类：**免版税**（VP8/VP9/AV1/Opus/FLAC）与**有专利池**
  （H.264/H.265/AAC），并写明"专利看方法不看实现"和"义务落在分发方"。
- `SECURITY.md` 填上真实联系邮箱和实际的版本支持策略。

**下一步（他提的方向）**：把这些声明**做进软件里** —— 计划是在设置页的「关于」
类目下加一个「开源许可」页，显示：版权与项目许可、第三方组件清单、以及各许可全文
（全文直接从 exe 旁边的 `licenses/` 读，不重复塞进 qrc；读不到就提示文件缺失）。
措辞要走语言文件（键名 `ui_legal_*`），中英各一份。

### 5. 源码版权头的**标准写法**（加新文件时照这个来）

每个源文件开头两行，**必须一致** —— 三处（源文件、`NOTICE.md`、发布包里的
声明）写的不一样，比不写还麻烦：

```
// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (C) 2026 Wang Yunzheng <wyz-mcast@outlook.com>
```

**注释符号按文件类型换**（内容不变，只换符号）：

| 文件 | 符号 |
|---|---|
| `.cpp` / `.h` / `.qml` / `.rc` | `//` |
| `CMakeLists.txt` | `#` |
| `.qrc`（XML） | `<!-- -->` |
| `lang/*.json` | **不加** —— JSON 没有注释语法 |
| `third_party/**` | **不加** —— 不是我们的代码 |

批量加/补的脚本：`work\add-spdx.ps1`（**幂等**，已有的会跳过；保原编码、保原换行）。
加完要重编 + 真跑一次新界面 —— 注释本身不会出问题，但万一插错位置（比如插到
`<?xml?>` 声明前面、或插进 raw string 里）就未必。

`licenses/` 目录已经建好（4 份 GPL/LGPL 全文 + FluentUI 的 MIT + 组件清单
`THIRD-PARTY-NOTICES.md`），`deploy-mcast.ps1` 会把整个目录拷进发布包。

### 6. 把法律信息做进软件（进行中）

**结构已定**（他确认过的）：这套标签页**只是"设置 → 关于"的下一级**，范围仅限
"关于" —— 投屏页、设置页的其它类目都看不到它。

```
[顶栏 48px]
[设置页]
  ├── 左侧类目：界面 │ 投送 │ 关于          ← 左栏的"关于"还在，不重复
  └── 右侧内容区（选中"关于"时）：
        [关于 │ 法律 │ 开源许可]             ← 只在这里出现的标签栏
        [当前标签的内容]
```

**三页各放什么**

- **关于**：就是现在设置页里那四张卡片（设备名称 / 地址 / 标识 / 版本），
  **原样搬进第一页，内容不动**。
- **法律**：顶部一段**跟随语言的通俗说明** —— 不是法律条文，是"这软件联不联网、
  在本机留下什么"的人话（法律文本没人读，这段才是用户真会看的）。下面每份文件
  一行、点击查看全文，**跟最终用户相关的排最上**（隐私 → 法律 → 商标 → 编解码器）。
  行标题规则：语言文件里有本地化名就显示 `本地化名 (English Name)`；没有就只显示
  英文名、不加括号。**括号由程序合并，语言文件控制不了** —— 否则每种语言都得手写
  括号，迟早不一致。英文名是权威标识，本地化名只是别名。
- **开源许可**：本项目自己那条**单独置顶**、与下面留间隔；第三方按**名称首字母**排。
  每行显示：项目名 / 版权方 / 采用的许可 / 右侧一个「查看许可证书」按钮。
  **必须能看到源码在哪** —— GPL 和 LGPL 都要求"提供对应源码的获取方式"。

**界面已完成（2026-09-14 凌晨，五个界面都截图验过）**

新文件都在 `src/ui/qml/legal/`：

| 文件 | 是什么 |
|---|---|
| `AboutTabs.qml` | 那三个标签（关于 / 法律 / 开源许可）+ 标签条 |
| `AboutPane.qml` | 第一页：原来设置页里那四张卡片，**原样搬过来** |
| `DocPane.qml` | 第二页「法律」：通俗说明 + 四份文件一行一份，点开看全文 |
| `LicensesPane.qml` | 第三页「开源许可」：本项目置顶 + 第三方首字母序 |
| `TextViewer.qml` | 看全文那一页（返回 + 标题 + 可滚动正文），两个页面共用 |

实测的样子：

- 「法律」页的行标题正是 `隐私说明 (Privacy Notice)` —— 本地化名 + **程序加的**括号；
  英文那边没给本地化名，所以只显示 `Privacy Notice`（语言键数 84 : 80，差的四个就是它们）。
- 「开源许可」页：`Media Cast Receiver` 单独置顶，空 16 像素，然后
  `FluentUI` → `libmpv / FFmpeg` → `Qt 6`。
- 「查看许可证书」当场读 `licenses/` 下的全文；↗ 用系统浏览器打开项目页。

**还没做的**

- **独立弹出窗口**：页面本身已经是"不认识窗口"的普通 Item，架构上就绪；但"装进独立
  `Window` 的宿主"还没写 —— 等首启向导要用时再做（复用 `AppTopBar` 那套
  `setHitTestVisible` 经验就行）。
- 那几份 `.md` 是**按纯文本显示**的（`#`、`**` 原样露出）。这是**有意的**：为这一个
  页面引入 Markdown 渲染不值得。要改也是后话。

**已完成（数据层 + 打包 + 自检）**

- `src/core/LegalDocs.{h,cpp}`：给界面三样东西 —— `documents`（4 份声明，按"用户
  相关度"排好）、`components`（3 个第三方，**首字母序**）、`project`（本项目自己
  那一条）；另有 `readDocument()` / `readLicense()` 从 exe 旁的 `legal/`、`licenses/`
  读全文。
- **排序规则放在 C++、不放界面** —— "本项目置顶、第三方按首字母"是**规矩**，不是
  排版偏好，所以只有一处来源。
- 构建的 post-build 会把 `docs/legal/` 里**面向用户的那 4 份**挑进构建目录的
  `legal/`，并整份拷 `licenses/`；`deploy-mcast.ps1` 改成从**构建目录**拷这两份
  （和 exe 一起出来的就是最终内容）。
- **启动自检**：`LegalDocs::scan()` 查这些文件在不在并写日志，缺了直接点名 ——
  发布包缺许可文本是合规问题，不该等用户点开才发现。实测输出
  `法律文本：4 份声明、3 个第三方组件`，无缺失警告。

**这一轮踩的两个坑**（都记着，别再犯）：

1. `Q_PROPERTY(... READ documents ...)` 写了 READ、**却没声明那个 getter** ——
   moc 生成的代码里找不到成员，编译报 `C2039`。属性三兄弟（documents / components /
   project）都得有对应的 `QVariantList documents() const`。
2. **自检不能放在构造函数里**：日志是构造之后才接上的，构造函数里 emit 等于白喊。
   和 `Tr` 一个套路 —— 先构造、接日志、再 `scan()`。

**从二进制里查证过的许可事实**（省得下次重查）：

| 事实 | 证据（在二进制里查到的原文） |
|---|---|
| libmpv 是 **GPL**，且**不是** GPLv2-only | `mpv is distributed under the terms of the GNU General Public License Version 2 or later.` |
| 它这一份构建启用了 GPL 组件 | 特性表里有 `gpl`，并有 `dvdnav` `dvdread` `libbluray` |
| 捆进来的那些库是 **LGPLv2.1+** | 5 处 `... either version 2.1 of the License, or (at your option) any later version` |
| mpv 版本 | `mpv v0.41.0-1023-g69e63f425`（构建选项里 `-Dlibmpv=true -Ddvdnav=enabled`）|

**"v2+" 和"拒绝 v3"是两件事**：mpv 写的是"v2 或更高"，也就是**允许接收者按 v3
用**。正因为如此，我们才有权把项目定成 `GPL-3.0-or-later`。（真正"避开 v3"的项目
是写成 v2-only 的，理由通常是：不想被 FSF 未来单方面改的条款绑住 —— Linux 内核
就是这条；以及硬件厂商讨厌 §6 的反 Tivo 化条款。）

**顺带澄清一个容易混的点**：GPLv3 的专利条款（§11）保护的是"**项目参与者之间的
专利和平**"—— 由贡献者就其贡献所必需的专利授予你许可，且不得对下游主张。它
**完全不提供**来自第三方的保护：HEVC 那几个专利池的权利人从没往这个项目贡献过
代码，所以选 v2 还是 v3，对他们的许可费一分钱影响都没有。**别把 §11 当成编解码器
专利问题的解药。**

**② GPL 不只是"附一份文本"。** 分发二进制要同时给三样：许可全文、**对应源码的
获取方式**（我们没改 mpv/ffmpeg，指到上游对应版本即可）、以及修改说明（无）。
而现在桌面那份发布包**一份许可文本都没带** —— 并且本地也没有现成的可拷：
Qt 安装目录没有 `LICENSES`，`runtime/mpv` 和 `runtime/mpv-dev` 里也没有任何
许可文件。标准文本得自己去取。

**③ 第三方声明现在就做，不能"等定下来再说"。** 我们已经在分发了，包里实际有：

| 组件 | 许可 | 要做的 |
|---|---|---|
| `libmpv-2.dll`（内含 ffmpeg） | **GPL v2+** | 全文 + 源码获取方式 |
| Qt6 全部 DLL 与插件（含 Qt5Compat） | **LGPL v3** | 全文 + 版权行 + "未修改"说明 |
| FluentUI（QML 组件库） | **MIT**（zhuzichu, 2023） | 全文 + 版权行 |
| `D3Dcompiler_47` / `dxcompiler` / `dxil` / `opengl32sw` | Qt 随附的第三方 | 各自条款 |

做法：包里加一个 `licenses/` 目录，一个组件一份。**Qt 是动态链接**，这满足
LGPLv3「用户能替换库」的要求 —— **所以千万别改成静态链接**，那是另一套义务。

**④ `CODECS.md` 该分成两类写。** 现在把 AV1 和 H.265 并列，但它俩性质完全不同：

- **明确免版税**：VP8、VP9、AV1（Google 和 AOMedia 都有公开承诺）
- **有专利池**：H.264/AVC、H.265/HEVC、AAC

分开写对用户更有用，也更诚实。

**⑤ `PRIVACY.md` 可以写实，因为这事我们最清楚。**
本程序**没有遥测、不联网上报**；只监听本地端口（TCP 8200、UDP 1900），只写本地
日志；日志里会带计算机名和媒体 URL（本地文件路径）。把这些写具体比模板的空话有
价值。`LEGAL-NOTICES.md` 第 5 节（网络）同理 —— 可以补上"会把计算机名广播到局域网"，
那是用户真正该知道的事。

**⑥ 这些文件将来要挪到仓库根目录。** `LICENSE` / `NOTICE` / `CONTRIBUTING` /
`SECURITY` / `CODE_OF_CONDUCT` 按惯例放根目录，平台才会识别。现在先攒在
`docs/legal/`。

**⑦ 关于 H.265 —— 这是专利问题，和版权是两码事。**

- **换实现躲不掉。** 专利覆盖的是"解码/编码 HEVC 这件事"这个方法本身，所以自己写、
  换库、换语言都一样。这也意味着"我们自己不写编解码器"并不构成免责理由。
- **池子不止一个。** Access Advance（原 HEVC Advance）、Via LA（原 MPEG LA 的
  HEVC 池）、Velos Media，外加不在池里的独立权利人。所谓"拿到许可"通常要谈好几家。
- **义务落在"分发方"身上，这条最关键。** 我们现在**随包分发 libmpv**，而它里面有
  HEVC 软件解码器 → **我们就是分发方**。如果改成"用户自备 mpv、我们只负责连上去"，
  分发方就不是我们了 —— 这是很多开源项目实际采用的方式。
- **光改用 GPU 硬件解码不免责**：解码器代码仍然在我们分发的二进制里。
- **别只盯着 H.265**：H.264/AVC、AAC 同样在池子里。
- 风险是分级的：个人自用和小范围免费分发，实际被追责的概率很低（VLC、mpv、Kodi
  这些业内老牌都这么分发）；**收费、预装、面向企业分发是完全不同的量级** ——
  那才是真正会发生诉讼的地方。

三条现实路线，按"代价"排：**现在**不动代码，只把许可文本和说明补齐、把
`CODECS.md` 写准；**要公开发布安装包**时，考虑不分发 mpv（用户自备）或提供
"不含 HEVC"的构建；**要商业化**就必须买许可或砍掉软件解码、改走平台解码器。

### 7. 项目地址（GitHub）

`https://github.com/JerryABCD06/MediaCast` —— **目前是空仓库，一次都没推过。**

这条牵出三件事：

1. **推源码之前，别对外发这个程序的二进制。** GPL 要求分发时提供"对应源码的获取
   方式"，而地址指向一个空仓库等于没提供。`LegalDocs` 里那一栏现在填的是真地址，
   但**只有推上去之后它才算数**。
2. **本机直连 github.com 不通** —— 实测 `git ls-remote` 报 `Connection was reset`，
   要走代理。将来 push 之前记得**给 git 单独配代理**：系统代理不一定被 git 用上
   （FlClash 那类工具默认不接管 git 的直连）。
3. **GitHub 账号是 `JerryABCD06`** —— 提交署名可以用 noreply 形式
   `ID+JerryABCD06@users.noreply.github.com`（它**收不到信**，所以联系方式
   仍然用 `wyz-mcast@outlook.com`）。

推送由他定时机，大概是：`git remote add origin <url>` → 配代理 → `git push -u origin master`。

---

## 二、没定的 / 要再想的

- **「像不像标题」那道筛子会误杀真标题（要不要放宽，得您定）。** 实测：
  BubbleUPnP 给视频的 `dc:title` 是 `Screenrecording_20260911_000943`，
  被 `looksLikeATitle` 判成"不像标题"跳过（罪名是"连续 5 位以上数字"，
  它命中了 `20260911`），最后界面显示类型名「视频」。
  **vivo 相册给的标题也长这样**（`Screenrecording_20260911_000943.mp4`，
  还带扩展名）—— 两家都一样，说明这不是个别现象。
  而**手机相机/截图的文件名几乎全是这个形状**（`IMG_20260911_123456`、
  `Screenshot_20260905_232916`、`VID_...`）—— 也就是说这道筛子在最常见的
  那类内容上会稳定失效。当初定它出来是为了挡 `1000140373` 这种内部编号，
  代价当时没量到。三个方向：把连续数字的阈值提高（比如 10 位以上才算编号）、
  只对"纯数字+分隔符"的串判成编号、或者干脆对**协议给的**标题放宽、
  只对"从 URL 推出来的"用严规矩。
- **自己画的云母底有几处取不到色，退回那块灰（#F3F3F3）：** 桌面背景是纯色、
  幻灯片、或者 Wallpaper Engine 那种第三方动态壁纸时，`SPI_GETDESKWALLPAPER`
  给的路径可能没有内容。另外壁纸铺法我们按"填充"算（Windows 默认也是它），
  桌面设成"平铺 / 居中 / 适应"时这份底和**真实桌面对不上** —— 但窗口里各块底
  之间仍然对得上（它们本来就是同一张图上裁下来的）。

  （原先这里还有一条"开关按 `availableEffects` 显示、所以 Win10 藏起来"——
  已经放开了：底色是我们自己算的，Win10 一样能用，藏起来只是把人挡在外面。
  同样的门槛在 `MicaBackdrop.micaOn` 和 `SettingsPage` 里都去掉了。）
- **加第二个协议之前必须先做的一步：把界面从 `DlnaRenderer` 上摘下来。**
  `TrayIcon` / `MainWindow` / `WindowsMediaControls` 现在都直接 include 了
  `protocols/dlna/DlnaRenderer.h`，手上只认识这一个门面。后果：新协议会有
  自己的门面，而界面只认第一个 —— **第二个协议投过来的东西，界面看不见、
  也控制不了**。

  要解开就得抽一层中性的"接收器"接口（谁连着 / 在放什么 / 命令往哪发 /
  勿扰开关），界面只认它，DLNA 和 AirPlay 各自去实现。**这和"把协议搬到
  后台线程"其实是同一件事的两面**：界面既不该知道协议在哪一层，也不该知道
  它跑在哪根线程上。

  （中立层本身是干净的：DIDL 的字段名、SOAP/GENA/SSDP 那些词、数值换算，
  全在 `protocols/dlna/` 里；`core/` 一个协议头文件都不 include。
  这次顺手把 `PlaybackController` 日志里写死的 "DLNA 投送" 也摘掉了 ——
  协议名现在由协议层自己填。）
- **歌词：读进来了，但还没有地方显示它。** 文件标签里的歌词会落到
  `NowPlaying.lyrics`（并暴露给 QML），日志里也会报一行"文件自带标签：…"。
  缺的是个界面（控制栏？独立面板？）和"歌词怎么排版"的定夺。
  另外**"歌词那个键到底叫什么"没验证成**：mpv 自带的编码器写不进自定义标签
  （ffmpeg 的封装器只认自己那张键表，`lyrics` 被丢掉了），所以造不出样本。
  要看准的话，投一首带内嵌歌词的歌，日志里那行"文件自带标签"会直接把它
  的名字和字数打出来。
- **陈旧订阅**：手机不是正常退订、而是直接走掉（关 Wi-Fi、没电）时，界面会
  一直显示"已连接" —— GENA 订阅有 30 分钟超时，我们不会主动去问它还在不在。
  日志里已经有一句"写完了，但对方一直没回话"，说明**我们知道对方没接**，
  只是没拿这个当成"它走了"。可以考虑加一条"连续几次推不动就当成断开"，
  那样手机走出范围时电脑这边能自己回到"未连接"。
- **网络协议要不要拆到后台线程**：**现在一个都没拆，协议全在主线程上** ——
  `SsdpService`（QUdpSocket）、`HttpServer`（QTcpServer）、`GenaManager` 都是
  Qt 的异步 socket 对象，靠事件循环收发，平时不占线程；代价是哪次解析或者
  处理卡一下，界面会跟着一起卡。将来协议多了（AirPlay / LeLink…）可以把整个
  协议层搬到一个带事件循环的 `QThread` 上 —— 它们都是 Qt 的 socket 对象，
  搬得动。**他说"到时候再说"，先记着。**

  > 顺带把线程现状记在这儿，省得下次再查一遍：整个进程里**只有 `MpvCore`
  > 的事件线程是我们自己开的**（`std::thread`，跑 `mpv_wait_event`，信号用
  > 队列连接送回主线程）；mpv 内部还有自己的线程；QML 的绘制在 Qt Quick 的
  > 渲染线程上（`MpvQmlItem` 的 render 就在那儿）。**托盘、旧界面、新界面
  > 三块都在主线程** —— 它们分别是 QSystemTrayIcon/QMenu、QWidget 和
  > QQuickWindow，Qt 要求它们必须待在主线程，挪走会崩。
- **关窗确认框的宽度写死 560。** 库自带的是 400，而它的高度是按"消息占一行"
  算的 —— 英文那句在 400 里折成两行之后**第二行会被切掉**，按钮上的
  "Close and disconnect" 也会被挤出框外。以后加语言如果哪一句更长，
  先看 `NewUiWindow.qml` 里那段注释。
- **顶栏样式**：现役是 `FluPivot`，观感偏 Windows 10。要 Win11 的样子得用
  我们自己写的 `TopNav.qml`（支持图标、支持左右分列）—— 它现在是**停用**
  状态，没进 `.qrc`，文件头和 `.qrc` 的注释里写了怎么启用。

---

## 三、已知故障

### 见到过一次、复现不出来的：音量显示成 0

2026-09-14 13:41 前后，`RenderingControl#GetVolume` 读到过 **0**（正常应当是 100）。
当时的状态：新界面开着、视频自然放完过一轮、窗口关掉又重开过。之后**反复试了四种
时序都没能再复现**：

1. 刚启动（没投任何东西）→ 100
2. 投视频期间 → 100
3. 视频自然放完 → 100
4. 放完关窗、再投重开 → 100

也排除了"音量滑块初始化时把它写成 0"（弹出层开之前/之后各读一次，都是 100）。

**先记着**。再见到的话重点看：那一刻播放器是不是被重建过、以及旧界面那个音量滑块
当时的值（它有一条 `valueChanged → setVolumePercent` 的连接）。

**2026-09-14 补一条线索**：那天 13:41 前后正是 FluSlider 那版滑块刚接上、在反复
点它试的时候。而 FluSlider 的纵向点击映射是反的（点顶端给最小值 0）—— 点一下、
松手就把 0 写回播放器，正好能读出 0。换成自己画的滑块之后，**顶端恒等于 100
（量过）**，写不出这个 0 了。没把当时的现场留全，所以只能说"能解释"，不敢说
"就是它"。

### Qt Creator 一打开这个工程就卡死

**症状**：Qt Creator 启动后主窗口立刻无响应（Windows 判定 `IsHungAppWindow`），
一直吃满一个核。实测盯了 30 分钟没恢复：

```
样本  1   CPU累计   502.0s  内存 609MB  响应=False
样本 90   CPU累计 2,178.5s  内存  42MB  响应=False
```

内存一路从 609MB 降到 42MB —— 它在死循环里把东西都释放了，但出不来。

**已排除**：不是编译慢（编译时根本没有编译器在跑，而且命令行构建 94 秒就完）、
不是内存不够（内存一直在降）、不是构建输出太多。

**怀疑对象**：给工程加进 FluentUI（180 个源文件，含 1.3 MB 的
`qcustomplot.cpp`、662 KB 的 `Chart.js`）之后才出现的。

**下一步验证**（30 秒，不用编译）：把 `CMakeLists.txt` 里的
`add_subdirectory(third_party/FluentUI ...)` 暂时注释掉，重新配置，打开工程 ——
如果 Qt Creator 不卡了，怀疑成立。

**不挡路**：命令行能编能跑。

---

## 四、搁置的小事

按"想起来再做"排序，都不紧急：

- **新界面的「播放器未就绪」占位文字**：条件写成了 `!Player.running`，而
  `running` 是"引擎起来了没有"—— 引擎开机就在跑，所以这行字永远不显示。
  他说无所谓。
- **用户设置文件的位置**：现在和 exe 同级。计划挪到
  `%LOCALAPPDATA%\MediaCast\`（config / renderer / logs / cache）。
- **媒体面板的程序名和图标**：试过 AUMID 没成，线索是要加开始菜单快捷方式。
- **AVTransport:2 的 `SetStateVariable` / `GetStateVariable`**：
  DLNA 唯一还欠的协议项。
- **`%LOCALAPPDATA%\CrashDumps` 里堆了 10 个 dump（合计 261 MB）**，可以清。
  其中 9 个是 MCast 的（前几轮那些崩溃留下的），1 个是 MediaControls 的。
- **`lang/README.md` 也会被拷进构建目录**（CMake 拷的是整个 `lang/` 目录）。
  开发时方便，发布时带不带进包，到时候定。

---

## 五、建议顺序

1. **语言系统最后一步**（媒体面板那几句）—— 半截活先收尾
2. **新界面的播放控制**（音量滑块 / 显示效果窗口 / 全屏 / 接真标题）
3. **陈旧订阅**那条（要动 GENA，先想清楚再动）
4. **旧界面退场 + 发布目录整理**

Qt Creator 那个死循环可以插空做（半小时的排查），不挡路。

---

## 六、环境备忘

**构建**：Qt 6.11.2 MSVC2022 64bit / Debug。

> ⚠️ **改过任何 `.h` 之后必须删掉整个构建目录重建。** 增量构建会漏编依赖文件，
> 表现为"编译链接全成功、一跑就崩"或者链接期报"无法解析的外部符号"。
> 这一条已经踩过好几次了。

**第三方**：`third_party/FluentUI` 是独立 clone，**不在版本控制里**。用的哪个
提交、我们改了它哪几行，见 `third_party/PATCHES.md`。

**翻代码前先看**：`git log --oneline`、这份文件、`third_party/PATCHES.md`。

### 界面上的几个时间常数

散在各文件里、但改的时候要一起想的四个数（各自文件里都有注释，这里汇总一眼）：

| 常数 | 值 | 在哪 | 为什么是这个数 |
|---|---|---|---|
| 淡入淡出 | **167 毫秒** | `MediaBar` 的 `opacity` Behavior | Fluent 这套库自己用的就是它（库里 39 处动画都是） |
| 静止画面收回 | **3 秒** | `MediaBar.idleTimer` | 他定的"停手、或者挪到界外，几秒后再收" |
| 新视频先露 | **5 秒** | `MediaBar.holdTimer` | 他定的"投新视频时先固定显示 5 秒" |
| 单击等待 | **250 毫秒** | `CastPage.clickTimer` | Windows 判断双击的间隔；为了不和"双击 = 全屏"打架 |

### 别踩的坑

- **切全屏会让整个窗口渲染不出来（全黑），退出也回不来。** 症状是：切进全屏之后
  窗口铺满、样式也对、客户区几何也正常，但**里面什么都没画**（全黑），Esc 退出后
  还是黑的。

  **治法**：**切之前先 `visibility = Window.Hidden`，再 `showFullScreen()`**；退出
  那一步同理。等于把渲染面丢掉、让它重建一次。

  **为什么**：Windows 把窗口换成无边框（WS_POPUP）时，Qt 那层渲染面不跟着重建。
  排查时逐个排除过的（都还是黑）：把 mpv 的画面 item 藏起来、关掉云母（不着色器）、
  去掉启动时那次离屏预渲染；也量过客户区几何是全屏大小、样式位正常；
  而**一个最朴素的 Qt 窗口（同样的 OpenGL 后端）全屏是好的** —— 所以问题在
  "这个窗口 + 库的边框助手"这一层，不在 Qt 本身。见 `NewUiWindow.qml` 里
  `enterFullscreen` 那段注释。

- **退出全屏的两种还原方式不一样。** 位置和尺寸**不要自己存、自己摆**：写过一版，
  量下来是多余的（Qt 自己那份"进全屏之前的普通几何"很准：335,18 → 全屏 → 335,18），
  而且自己存还会踩坐标系的坑。但**最大化**那档要**直接 `showMaximized()`、不要先
  `showNormal()`** —— 先 normal 再 maximized 会落回普通窗口（实测）。

- **"点画面"的单击要等 250 毫秒再执行。** 因为双击画面 = 切全屏，而 Qt 对一次双击
  会先发一个 `clicked`、再发 `doubleClicked` —— 实测单击那一下会把播放/暂停**真的
  切一次**（不是"切两次净效果不变"，我一开始这么以为，日志打了脸）。所以单击先
  记着，来了双击就取消它。**控制栏中间那个键不受影响，仍然是立即的。**

- **量窗口位置/尺寸的探针脚本，坐标可能被 Windows 换算过。** 那几个 `.ps1` 里
  `SetProcessDPIAware()` 叫得晚一点，`SetWindowPos(400,60)` 实际会把窗口摆到
  (500,75)（125% 缩放）。有一轮我把这个当成"程序退出全屏后位置偏了 84 像素"，
  查了半天 —— 其实是**探针自己的问题**。要判断这种事，用**同一个工具**在切换前后
  各量一次（`grab-qml-window.ps1` 会打印 `picked window 宽x高 at x,y`）。

- **关窗 = 真的销毁窗口对象，所以那个裸指针必须跟着清。**
  FluentUI 的 `FluWindow` 默认 `autoDestroy: true` 就会销毁窗口，而
  `NewUiWindow` 拿的是裸指针 `m_window` —— 它不会自己变空，下一次投屏进来
  调 `m_window->show()` 就崩在 Qt6Gui 内部读非法地址（`Qt6Guid+0x34BA22`，
  连崩四次才查清）。**别把 `destroyed` 那条清理去掉。**
  关窗策略写在 `NewUiWindow.qml` 的 `closeListener` 里：有投送先弹框问，
  确认了才真关。
- **同一个进程里加载第二个 `QQmlApplicationEngine` 会崩**（实测崩在 Qt6Qml
  的 `m_engine->load()` 上）。所以规矩是：**引擎一个进程只建一次，窗口可以
  建了又销毁**。改 `NewUiWindow::load()` 之前先看那段注释。
- **QML 里切页不能藏起 `MpvQmlItem`。** render API 靠每帧渲染推着走，
  一藏起画面 item，mpv 的视频输出就停了 —— 再开也起不来（表现是"状态显示
  在播、位置一直是 0、画面全黑"）。设置页是**盖上去**的，不是换掉投屏页。
- **不要用 `checked: X.y` / `value: X.z` 这类绑定去接开关和滑块** ——
  控件一被用户操作，绑定当场就断了。统一用 `Component.onCompleted` 起手 +
  `Connections` 跟外部变化 + 点击时写回。
- **`FluWindow` 的 `appBar` 上，我们自己的按钮必须单独
  `window.setHitTestVisible(item)`**，否则按下去是拖窗口。窗口那三个按钮
  `FluWindow` 已经替我们登记过了，**两组必须分开**。
- **`FluWindow` 用了 `Component.onCompleted`**，实例上再写一个可能把它顶掉
  （窗口就不显示了）—— 改用 `AppTopBar` 的 `ready()` 信号。
- **图标名要核对** `third_party/FluentUI/src/FluentIconDef.h`（踩过：
  `TvMonitor` 实际是 `TVMonitor`）。**而且名字会骗人** —— 代码里曾经写着
  "上一首 / 下一首没有实心版"，其实有，只是那对的名字叫
  `BackSolidBold` / `ForwardSolidBold`，不叫 `PreviousSolid` / `NextSolid`。
  光翻名单猜不出来，得**看形状**：`work\glyph-sheet.ps1` 把任意几个码位渲染成
  一张对照表（名字 + 码位 + 字形），看一眼就知道哪对是哪对。音量那五档
  （`Mute` / `Volume0`~`Volume3`）也是这么找出来的。
- **窗口"露面"到"画上第一帧"之间，那段时间窗口表面是透明的。** 用户看到的
  是：边框和阴影先出来，里面还是后面那些窗口，一秒多之后内容才刷上来
  （探针 `work/cap-flash.ps1`：窗口阴影出现之后，内部那个点还要 1.4 秒才变色）。
  **光声明 `Window.color` 没用** —— 那段时间根本没有渲染过任何一帧，清屏色
  没机会生效（试过，量的结果和改之前一模一样）。现在这么治：
  `autoVisible: false` + `visible: false`（**两个都要**：前者只管库自己那次
  show()，QML 的 Window 本身默认 `visible: true`）+ `NewUiWindow::show()` 里
  先 `grabWindow()` 离屏渲染一帧再 `show()`。改完透明那段降到约 0.1 秒，
  而且落在那扇窗自己的淡入动画里。代价：窗口出现晚约 2 秒（这份初始化开销
  从"露面之后"挪到了"露面之前"）。
  壁纸那张图也跟着改成**同步解码**（`asynchronous: false`）—— 否则会先看到
  一小会儿灰底再变成云母（量到约 0.3 秒，改完约 0.1 秒）。
- **Qt 6 的 QML 里，`Screen` 没有 `geometry` / `virtualX` / `virtualY`。**
  那是 Qt 5 的 API，Qt 6.11 的 `Screen`（`Window.screen` 和贴附属性都一样）
  只有 `width/height/desktopAvailableWidth/Height/devicePixelRatio/...`。
  写了一不会报错，只会拿到 `undefined`，然后**整块底变成一张空图** ——
  日志里那行 `Unable to assign [undefined] to QRectF` 是唯一的线索。
  库自己那份 `FluWindow` 的背景（`com_background`）里还在用
  `window.screen.virtualX`，所以在 Qt 6 下**它那条彩色的云母路径本来就是坏的**。
  我们这边：尺寸用贴附属性 `Screen.width/height`，屏幕原点用
  `FluTools.desktopAvailableGeometry(window)`（任务栏在右侧/下侧时它是准的，
  在左侧/上侧会差一个任务栏的厚度 —— 过一遍模糊就看不出来了）。
- **`ShaderEffectSource` 采不到 `visible: false` 的源。** 写 `visible: false` 的
  壁纸图，采到的是一张**空图**，模糊出来什么都没有（屏幕上只剩染色层盖在底色上，
  看着像一块发白的灰）。要么让源保持可见（我们让亚克力整个盖住它），
  要么用 `hideSource`。
- **祖先的 `clip` 会污染 `ShaderEffectSource` 的源渲染。** Qt 把源 item 渲染进
  纹理时连裁剪一起算，于是顶边那圈用来当"模糊邻域"的像素是空的 ——
  页面上的底和窗口那层因此对不齐，接缝上一条横线。**壁纸图必须放在不被裁的
  那一层**（我们放在窗口背景里，页面只借用它）。
- **QML 里别用 `mapToItem` 算位置，也别把位置算在"只在某个属性变化时才重算"
  的绑定里。** 实测 `mapToItem(win.contentItem, 0, 0)` 恒返回 `(0,0)`
  （把父级 x/y 一层层加起来得到的是正确的 48）；而且绑定只认**被读到的属性**，
  `mapToItem` 在 C++ 里读 item 自己的 x/y，引擎看不见 —— 锚点又是创建之后才落的，
  于是算一次就定死。`MicaBackdrop.originInWindow` 现在是自己沿 `parent` 累加。
- **脚本里的 exe 路径别写死构建目录名。** 清掉旧构建目录之后，那些
  `mcast-build2` / `mcast-build5` 之类的写死路径全烂了，症状是"程序起来了
  又立刻没"或者一串"目标计算机积极拒绝"。现在统一指向 `mcast-build11`。
- **PowerShell 变量名大小写不敏感 —— 参数叫 `$X` 的时候，循环里的 `$x` 就是同一个变量。**
  写了个探针：参数 `$X/$Y/$W/$H`，循环 `for ($x = $X; $x -lt ($X + $W); $x++)`。
  循环体里 `$x++` 改的就是 `$X`，于是"终点"跟着往前跑，**跑成死循环**，
  输出刷了几百万行报错才被掐掉。探针脚本里的参数现在叫 `$RoiX / $RoiY / …`。
- **窗口最小化之后 `GetWindowRect` 给的是屏幕外的坐标**（这台机器上是
  `-25600,-25600`、尺寸也不对），而 `IsWindowVisible` **照样返回真**。
  按这种坐标算出来的点击全落在屏幕外，**一点效果都没有、也不报错**。
  选窗口时要连 `IsIconic` 一起看：最小化的先 `ShowWindow(h, 9)`（SW_RESTORE）
  恢复，或者干脆跳过。
- **`FindWindowW` 在这个会话里按标题找不到东西** —— 连
  `FindWindowW($null, $null)` 都返回 0（而进程里明明有那个窗口）。`EnumWindows`
  是好用的，所以脚本一律走 `EnumWindows` + 自己筛。同一个进程里还有两个标题都叫
  `Media Cast` 的顶层窗口（QML 的新界面类名 `…QWindowOwnDCIcon`、旧 Widgets 的
  是 `…QWindowIcon`），**别只按标题挑**。
- **`FluWindow` 的 `background` 要的是 `Component`，不是 Item。** 写成
  `background: Rectangle { … }` 会得到 `Unable to assign QQuickRectangle to
  QQmlComponent`（日志里那行就是），后果是**那层底根本没建起来、窗口是透的** ——
  主窗口和内容之间能看到别的窗口。正确写法：`background: Component { … }`。
- **QML 的相对 import 少一句，炸的是整个界面。** `picture/PictureWindow.qml` 要用
  `components/` 里的 `MicaBackdrop`，得写 `import "../components"`；漏了是
  `MicaBackdrop is not a type` → `Type PictureWindow unavailable` → **新界面整体
  加载失败**（不是只有那一个窗口坏掉）。和 `Popup` 要 `import QtQuick.Controls`
  那条是同一个道理：QML 是整棵树一起编的。
- **`FluRectangle` 的 `radius` 是"四个角各一个数"的列表，不是单个数字。**
  写 `radius: 6` 只会圆**左上角**，另外三个角是直角（它是 C++ 画的：
  `paint()` 里按 `r[0]` 左上 / `r[1]` 右上 / `r[2]` 右下 / `r[3]` 左下 取，
  不够的补 0）。正确写法 `radius: [6, 6, 6, 6]`，库自己那几处也是这么写的
  （`[5,5,5,5]`、`[6,6,0,0]`）。音量浮出控件那个框就是这么歪了半天的。
- **要在"别的东西上面"接点击，先找有没有相对坐标可用 —— 别急着算绝对坐标。**
  音量面板那个静音键，第一版是"给整窗模态遮罩挖个洞"，于是必须知道那个按钮在
  **窗口**里的位置：沿 parent 链累加、`mapToItem` 两种算法给出同一个值，可它和
  屏幕上量出来的差 48 像素，查了一轮没查出所以然（那 48 正好是顶栏高度）。
  后来换成**在那个弹层里摆一块盖上去**：`Popup.x/.y` 本来就是相对父项的，取相反数
  就到位了，窗口坐标一个都不用碰。**能站在别人肩上就别自己搭梯子。**
- **窗口里的东西能不能滚，别只看截图。** 这一页的内容是 646 逻辑像素、视口 444，
  确实能滚（他自己一滚就动了）；但脚本注入的滚轮一动不动 —— 因为
  `WM_MOUSEWHEEL` 是送给**焦点窗口**的，不是送给鼠标底下那个窗口的。真要脚本滚，
  得先点一下窗口里没有控件的地方把它激活。**不过现在界面操作归他本人做**（见下面
  「模拟器测试台」最后那条），脚本只用来读状态和抓图。
- **别用 `monkey -c android.intent.category.LAUNCHER` 启动模拟器里的 App。**
  实测它把 MuMu 自己的"游戏中心"和"设置"给拉起来了（症状：`mCurrentFocus=null`，
  屏幕上是游戏中心）。原因是电视类应用（AirScreen 这种）的入口挂的是
  **LEANBACK_LAUNCHER** 而不是 LAUNCHER，monkey 匹配不到就开了别的。
  稳的写法是先问系统：`cmd package resolve-activity --brief <包名>` 拿到入口组件，
  再 `am start -n <组件>`。`mc-adb.ps1` 的 `Start-McApp` 已经改成这个顺序了。
- **别用固定坐标点模拟器里的界面。** 坐标会随着"上一个页面停在哪儿""设备列表里
  多了个设备"整体错位，而且点空了看不出来（表现为"什么都没发生"，很难查）。
  要么按文字找控件（`Find-McNode`），要么**人点、我们只负责量** —— 后者反而最快，
  配 `start-cap.ps1` / `stop-cap.ps1`（抓包丢在客机里跑，不受会话影响）。
- **同一台安卓里两个 App 之间的通话走 loopback**，抓 `wlan0` 是 0 个包，
  要抓 `lo`。
- **跑在模拟器里的接收器会被 Android 冻结**：切到后台后它对外还宣称老端口、
  实际已经不监听（现象是一串 RST）。MuMu 设置里的「应用保活」打开就稳了。
  另外这类接收器的 UPnP 端口**每次重启服务都会换**，别缓存。
- **`Popup` 要 import `QtQuick.Controls`。** 少一句就是 `Popup is not a type`，
  而且**整个新界面加载失败**（不是只有那一个按钮坏掉）—— QML 是整棵树一起编的。
  实测症状是窗口根本不出来。`MediaBar.qml` 原来用不上 Controls，加音量滑块时漏了。
- **弹出层盖住画面时，点外面那一下会漏下去。** Qt 的 `Popup` 默认 `modal: false`：
  它负责关自己，**不负责吃掉那一下点击** —— 于是"点画面 = 播放/暂停"被触发。
  实测过：弹出层开着时点画面空白处，传输状态从 `STOPPED` 直接变 `PLAYING`。
  治法两条一起上：`modal: true`（挡点击），再把 `Overlay.modal` 换成一个空的
  `MouseArea` —— **默认那层遮罩会变暗**（给对话框用的），实测整窗明显暗一块。
- **弹出层开着的时候，得告诉控制栏"别收"**（`MediaBar.popupOpen`）。不然鼠标从
  按钮往上挪到滑块上，那一瞬间既不在栏上也不在下沿热区里，栏直接淡出、滑块跟着没。

> 顺带：那些脚本起了 MCast 之后**不会**自动挂 Qt 的 DLL 目录，不挂的话
> 进程会立刻退出。`mpvcore-regression.ps1` 里已经补了一行，别的脚本大多
> 本来就有。

### 开发用的脚本和构建目录

都在 `C:\Users\WYZ\Documents\Codex\2026-09-11\nin\work\`：

```
mcast-build11\          当前唯一的构建目录（其他的都清掉了）
build-mcast.ps1         在构建目录里编（会自己导 vcvars64）
configure-mcast.ps1     重新配置 + 编（改过 CMakeLists 之后用）
captures\               抓的包（vivo.pcapng 是 vivo 相册那次的完整记录）
e2e-media\              测试用媒体
icon\                   图标资源 + 各种截图
*.ps1                   验证脚本（下表）
```

| 脚本 | 用途 |
|---|---|
| `mpvcore-regression.ps1` | DLNA 全套回归（投放 / 状态 / 进度 / 暂停 / 跳转 / 音量）。**末尾会杀掉程序** |
| `cast-file.ps1` | 往程序里投一条本地文件；`-Play` / `-Stop` / `-StopOnly` |
| `eof-watch.ps1` | 投上去之后每 2 秒问一次状态和位置（看"播完之后"用） |
| `eof-state-test.ps1` | 播完 → 抓图 → 点画面 → 抓图 |
| `eof-recast.ps1` | 播完再投下一条（验"换片子被上一条的暂停钉住"那个 bug） |
| `img-test.ps1` | 投图片，看它留不留得住 |
| `qml-click.ps1` | 按**窗口内相对坐标**点一下（坐标要靠截图量） |
| `qml-hover.ps1` | 把鼠标停在某个按钮上并截图（看悬停提示） |
| `tray-menu-grab.ps1` | 打开托盘的"隐藏的图标"面板、右键图标、截菜单图 |
| `grab-qml-window.ps1` | 抓 QML 窗口（屏幕 DC BitBlt，PrintWindow 抓不到 GL） |
| `crop.ps1` | 裁一块图并放大（最近邻），用来看像素 |
| `probe-popup-slider.ps1` | 扫音量浮出控件中轴那一列，把白底 / 轨道 / 已到的一段分段打出来 |
| `mclick.ps1` | 快速连点（先把鼠标依次移到几个点唤醒控制栏，最后一下是真点） |
| `vol-test.ps1` | 音量滑块的点测：点顶端 / 底端 / 正中，每步从协议侧读回真音量 |
| `pic-test.ps1` | 「显示效果」窗口：`-Open` 点开并抓图 / `-Grab` 只抓 / `-Click "x,y"` 点窗口内某点 / `-Wheel` `-DragScroll` |
| `soap-picture.ps1` | 画面调节的协议侧：`-Get` / `-Set -Name Brightness -Value 75` / `-Reset`（SelectPreset、DLNA 那套 0~100、50 才是中性） |
| `glyph-sheet.ps1` | 把图标字体里指定的码位渲染成一张对照表（名字 + 码位 + 字形）——挑图标前先看形状 |
| `vol-icon-states.ps1` | 音量按钮的五个状态：协议侧挨个设音量、每次抓图裁出按钮、拼成一条 |
| `gena-volume-burst.ps1` | 量"连续改音量"会推几条 GENA 事件（订阅 + 假装拖动） |
| `gena-pause-test.ps1` | GENA 推送（订阅 + 看 TransportState） |
| `soap-cmd.ps1` | 只发一条 Play / Pause / Stop（不重新投内容） |
| `test-bar-visibility.ps1` | 控制栏显隐规则的十种情形走一遍，每种截一张图 |
| `montage-bottoms.ps1` | 把几张截图的"底那一条"竖着拼成一张，一眼看谁有栏 |
| `watch-bar-hold.ps1` | 每 0.9 秒抓一次、读数，看控制栏亮了多久（"新视频 5 秒"用它验的） |
| `cap-flash.ps1` | 抓"窗口刚出现那一会儿"的帧，看有没有一段真透明（启动空窗期用它量的） |
| `clean-captures.ps1` | 清掉探针留下的截图/读数（`Remove-Item` 被策略挡，走的 .NET API） |
| `configure-mcast-release.ps1` | 配 Release 构建目录（`mcast-release`） |
| `deploy-mcast.ps1` | 打发布包（windeployqt + 拷到目标目录），见上文「3. 发布目录整理」 |
| `mc-adb.ps1` | **模拟器测试台**的基础层：截图 / UI 树 / 按文字找控件 / 点 / 打字 / 按键 / root / logcat |
| `wait-emulator.ps1` | 等模拟器开机完成并报它的地址、能不能通到渲染器 |
| `bubble-observe.ps1` | 跑一段真实操作（投放 / 暂停 / 恢复 / 拖进度），把渲染器日志 + 它的 logcat + 截图一起收进一个目录，并统计"同类请求的间隔" |
| `capture-soap.ps1` | **在客机里** tcpdump 抓 8200 端口，然后把 SOAP 请求体原样掏出来（看控制点发的 DIDL 用） |
| `start-cap.ps1` / `stop-cap.ps1` | 抓包拆成两半：start 让抓包在**客机里自己跑**（不受我这边会话影响），stop 拉回来解析。**人工操作时用这一对** |
| `bubble-cast.ps1` | 按**文字**找控件来驱动 BubbleUPnP 投放（坐标会错位，文字不会） |
| `ref-ab-test.ps1` | 对照用：同一个 App 在前台 / 在后台，它的 UPnP 端口和 SSDP 应答有什么不同 |

### 模拟器测试台（真机控制点，可脚本驱动）

用一台 MuMu 里的 BubbleUPnP 当**真实控制点**：它比手机强的不是"更像真机"，
而是**可脚本驱动、可重复、能同时拿到截图 + UI 树 + logcat**。它只负责"按真实
控制点的按钮"；注入畸形请求那半边得靠我们自己写的测试控制点（别混）。

环境（2026-09-14 打通）：

```
D:\Apps\MuMuPlayer\nx_main\adb.exe     用 MuMu 自带的 adb（版本对不上会掉线）
实例名「娱乐（ROOT）」                  KernelSU 的 su 可用：uid=0, u:r:ksu:s0
adb：10.180.157.202:5555               桥接后的局域网地址
BubbleUPnP：com.bubblesoft.android.bubbleupnp
```

**桥接模式有两个坑**（都踩过）：

1. **切桥接之后 `127.0.0.1:16384` 那条端口转发就死了** —— 它绑在 NAT 那条老
   路径上。而 `MuMuManager.exe info -v all` 重启后照样一口咬定
   `"adb_port": 16384`，实际上那个端口没人监听。别信它，改连客机的
   `局域网地址:5555`。
2. **怎么找到客机的地址**：`vms\<实例名>\macaddress` 里是它的 MAC
   （`0839A0E78F73` 这种），展开成 `08-39-A0-E7-8F-73` 去宿主 `arp -a` 里对，
   翻出来的 `10.x.x.x` 就是它。（没现成 ARP 条目就先 ping 一下网段。）

顺带记：模拟器里 FlClash 的 VPN 是开着的（`tun0` 接管全流量），但它的绕过列表
含 `10.*`，所以到渲染器的流量是直连的。以后 SSDP 行为怪，先怀疑它。

**他不能双击构建目录里的 exe**（缺 Qt DLL），每次都得用 PATH 挂上 Qt 的 bin
才跑得起来 —— 起程序这件事还是这边帮他做。

**但"点界面"这件事从 2026-09-14 起归他本人做**（他定的）：这边只负责起程序、
投测试文件、读日志、抓图。理由是脚本点界面本来就不可靠 —— 控制栏会自己收起来、
浮出控件是模态的会吃掉第一下、`WM_MOUSEWHEEL` 只送给焦点窗口 —— 而且脚本动的是
**他正在用的那套鼠标键盘**。所以流程是：这边说"请点 X，然后截个图"，他做完把
截图丢过来，这边接着读。
