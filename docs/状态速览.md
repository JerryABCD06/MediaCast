# 状态速览

> 给"上下文被压缩之后接手"用的一屏摘要。**这份只写短期不会变的东西**。
> 细节在 `待办.md`，协议实现情况在 `DLNA实现情况.md`，语言文件规矩在
> `lang/README.md`。改了什么、提交号是几 —— 一律用 `git log --oneline` 看，
> 抄进文件里第二天就过时。

最后更新：2026-09-13

## 这是什么

Windows 上的 DLNA MediaRenderer：手机（vivo 相册 / BubbleUPnP 等）投屏过来，
电脑这边放。播放器是 libmpv，画面用 render API 合进 QML 场景。
界面有两套：旧的 Qt Widgets（`ui/MainWindow.*`，留着当测试界面，将来退场）、
新的 QML + FluentUI 库（`ui/NewUiWindow.*` + `ui/qml/**`，主力）。

## 路径

| 东西 | 位置 |
|---|---|
| 项目根 | `D:\Me\WindowsTVReceiver\` |
| 源码（git 仓库） | `D:\Me\WindowsTVReceiver\source\MediaCastReceiver\` |
| 构建目录 | `C:\Users\WYZ\Documents\Codex\2026-09-11\nin\work\mcast-build11\` |
| 探针脚本、截图 | `C:\Users\WYZ\Documents\Codex\2026-09-11\nin\work\` |
| Qt | `D:\Apps\Qt\6.11.2\msvc2022_64` |
| vcvars | `D:\Apps\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat` |

## 怎么构建、怎么跑

- 构建：`work\build-mcast.ps1`（增量）；改了 CMake 或 `.h` 用
  `work\configure-mcast.ps1 -Build`（重配 + 全编）。
- 跑：exe 旁边没有 Qt DLL，**双击会弹缺 `Qt6Widgetsd.dll` 之类的框**。得先把它
  加进 PATH：`$env:PATH = 'D:\Apps\Qt\6.11.2\msvc2022_64\bin;' + $env:PATH`，
  然后 `Start-Process -FilePath '...\mcast-build11\MCast.exe' -WorkingDirectory '...\mcast-build11'`。
- 发布：`work\configure-mcast-release.ps1 -Build`（编 Release）→
  `work\deploy-mcast.ps1`（windeployqt 补齐 + 整份拷到桌面）。细节在 `待办.md`。
- 注意：`Start-Process` 加 `-RedirectStandardError` 起的进程会被回收掉 —— 要收
  stderr 就**在同一条命令里**起、跑完、再读文件。

## 现在能干什么

手机 DLNA 投屏 + 播放（图片 / 音乐 / 视频）、托盘常驻、勿扰模式、
Windows 媒体面板、新界面（画面区、顶栏、设置页、媒体控制栏、悬停提示、全屏）、
语言可切换（新界面 + 托盘 + 媒体面板一起变）。

## 分层

```
DLNA（protocols/dlna/）   ← 只管 UPnP/SOAP，不碰播放器
    ↓ 中性枚举
PlaybackController（core/）  ← 状态的唯一来源
    ↓
MediaPlayer（core/MediaPlayer.h，抽象接口）
    └── MpvCore              mpv 实例 + 事件线程 + 状态缓存 + 命令 + 画面调节
          ├── LibMpvPlayer   wid 外壳（旧界面用）
          └── MpvQmlItem     render API 外壳（新界面用，画面进 QML）

UI：ui/NewUiWindow.* + ui/qml/**（新） / ui/MainWindow.*（旧，测试用）
    界面只读 PlaybackController 的状态，不自己拼
```

## 关键文件

| 文件 | 管什么 |
|---|---|
| `src/core/PlaybackController.{h,cpp}` | 状态唯一来源：连接 × 在放什么 |
| `src/core/MpvCore.{h,cpp}` | mpv 那一层的全部实现 |
| `src/ui/qml/NewUiWindow.qml` | 窗口外壳、云母底、关窗策略、全屏 |
| `src/ui/qml/components/MediaBar.qml` | 媒体控制栏；**显隐规则和排版数值的权威注释在文件顶部** |
| `src/ui/qml/components/MicaBackdrop.qml` | 自己画的云母（造一次、各处只裁） |
| `docs/待办.md` | 现在到哪儿了 / 别踩的坑 / 探针脚本清单 |

## 会咬人的约束

1. **改过任何 `.h`，必须删掉整个构建目录重配重编**。增量构建会漏依赖，
   结果是编译链接全过、一跑就崩。
2. PowerShell 长命令会被策略拦；`Remove-Item` 被挡。**写 `.ps1` 再跑**，删文件用
   `[System.IO.File]::Delete` / `[System.IO.Directory]::Delete`。
3. 提交信息**写进 `work\commitmsg.txt` 再 `git commit -F`**（长 `-m` 会被编码搞坏）。
4. **QML 里切页不能把 `MpvQmlItem` 藏起来**（render API 靠每帧渲染推进）；
   要"藏"就缩成 1×1。
5. 开关、滑块**别用 `checked: X.y` / `value: X.z` 绑属性**，一点就断。
6. 新加的 QML 文件要同时写进 `res/QmlUi.qrc`，改完要确认 rcc 真的重编了。

## 界面上的几个时间常数

| 值 | 用在哪 |
|---|---|
| 167 ms | 控制栏显隐动画（Fluent 库自己的标准） |
| 3 s | 暂停 / 图片时鼠标停手后收起 |
| 5 s | 新投上来的视频先露一会儿 |
| 250 ms | 画面单击（播放/暂停）延迟执行，等双击 |

## 下一步

控制栏右侧那几个键还是壳子：**音量**（按钮上方弹纵向滑块）、**显示效果**
（独立窗口，搬旧界面那十多项）、**字幕**（功能没做）。上一首 / 下一首先别接。
