# 状态速览

> 给"上下文被压缩之后接手"用的一屏摘要。**这份只写短期不会变的东西**。
> 细节在 `待办.md`，协议实现情况在 `DLNA实现情况.md`，语言文件规矩在
> `lang/README.md`。改了什么、提交号是几 —— 一律用 `git log --oneline` 看，
> 抄进文件里第二天就过时。

最后更新：2026-09-14

## 这是什么

Windows 上的 DLNA MediaRenderer：手机（vivo 相册 / BubbleUPnP 等）投屏过来，
电脑这边放。播放器是 libmpv，画面用 render API 合进 QML 场景。
界面只有一套：QML + FluentUI（`ui/NewUiWindow.*` + `ui/qml/**`）。
**启动时不弹窗口**，只在托盘里待着；有东西投过来（或者托盘菜单点「打开主界面」）
才开窗口。（旧的 Qt Widgets 界面 2026-09-14 退场了。）

## 路径

| 东西 | 位置 |
|---|---|
| 项目根 | `D:\Me\WindowsTVReceiver\` |
| 源码（git 仓库） | `D:\Me\WindowsTVReceiver\source\MediaCastReceiver\` |
| 构建目录 | `C:\Users\WYZ\Documents\Codex\2026-09-11\nin\work\mcast-build11\` |
| 探针脚本、截图 | `C:\Users\WYZ\Documents\Codex\2026-09-11\nin\work\` |
| Qt | `D:\Apps\Qt\6.11.2\msvc2022_64` |
| vcvars | `D:\Apps\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvars64.bat` |
| 项目地址（GitHub） | `https://github.com/JerryABCD06/MediaCast` —— **目前是空仓库**，还没推过 |
| GitHub 账号 | `JerryABCD06`（提交署名可用的 noreply 形如 `ID+JerryABCD06@users.noreply.github.com`） |

> **本机直连 github.com 不通**（`Connection was reset`），要走代理。将来 `git push`
> 之前记得给 git 单独配代理 —— 系统代理不一定被 git 用上。

## 怎么构建、怎么跑

- 构建：`work\build-mcast.ps1`（增量）；改了 CMake 或 `.h` 用
  `work\configure-mcast.ps1 -Build`（重配 + 全编）。
- 跑：exe 旁边没有 Qt DLL，**双击会弹缺 `Qt6Widgetsd.dll` 之类的框**。得先把它
  加进 PATH：`$env:PATH = 'D:\Apps\Qt\6.11.2\msvc2022_64\bin;' + $env:PATH`，
  然后 `Start-Process -FilePath '...\mcast-build11\MCast.exe' -WorkingDirectory '...\mcast-build11'`。
- 发布：`work\configure-mcast-release.ps1 -Build`（编 Release）→
  `work\deploy-mcast.ps1`（windeployqt 补齐 + 整份拷到桌面）。细节在 `待办.md`。
- 注意：`Start-Process` 加 `-RedirectStandardError` 起的进程会被回收掉 —— 要收
  stderr 就**在同一条命令里**起、跑完、再读文件。

## 现在能干什么

手机 DLNA 投屏 + 播放（图片 / 音乐 / 视频）、托盘常驻、勿扰模式、
Windows 媒体面板、新界面（画面区、顶栏、设置页、媒体控制栏、悬停提示、全屏）、
语言可切换（新界面 + 托盘 + 媒体面板一起变）。

## 分层

```
DLNA（protocols/dlna/）   ← 只管 UPnP/SOAP，不碰播放器
    ↓ 中性枚举
PlaybackController（core/）  ← 状态的唯一来源
    ↓
MediaPlayer（core/MediaPlayer.h，抽象接口）
    └── MpvCore              mpv 实例 + 事件线程 + 状态缓存 + 命令 + 画面调节
          ├── LibMpvPlayer   wid 外壳（**现在没人用**，留着当退路）
          └── MpvQmlItem     render API 外壳（在用的就是它，画面进 QML）

UI：ui/NewUiWindow.* + ui/qml/**（唯一那套界面）
    界面只读 PlaybackController 的状态，不自己拼
    Windows 媒体面板挂在**一个常驻的隐藏窗口**上（不挂主界面那扇 —— 它关了会真销毁）
```

## 状态只有一份（界面照着读，别自己拼）

界面上会问的"局面"其实就两条，都算在 `PlaybackController` 里：

| 问什么 | 读哪个 | 谁在算 |
|---|---|---|
| 手机连着没有 | `Playback.peerConnected` | 协议层喂（有 GENA 订阅、且不是我们主动挂断） |
| 在投送视频 / 音乐 / 图片，还是没在投 | `Playback.castState` | `peerConnected` × 内容类型 × `idle`，就那十来行 |

消费方：顶栏状态胶囊（文字 + 颜色）、投屏页的引导语标题 —— 读 `castState`；
顶栏「断开连接」按钮的显隐、关窗确认框 —— 读 `peerConnected`。
**"手机连着没有"这一件事，代码里从头到尾只有一种写法。**
（`2026-09-14` 逐个查过：原先那两处分别写的是 `castState !== NoViewer` 和
`hasMedia`，都对、但都是绕着的说法，已经统一成 `peerConnected`。）

更细的那几个量（`showsPicture` / `mediaIsVideo` / `paused` / `hasMedia` / `idle`）
是**别的问题**用的（屏幕上有没有画面、控制栏该用哪套颜色、点一下有没有意义），
各自在头文件的注释里写明了为什么不能用上面那两条替代 —— 别混。

## 关键文件

| 文件 | 管什么 |
|---|---|
| `src/core/PlaybackController.{h,cpp}` | 状态唯一来源：连接 × 在放什么 |
| `src/core/MpvCore.{h,cpp}` | mpv 那一层的全部实现 |
| `src/ui/qml/NewUiWindow.qml` | 窗口外壳、云母底、关窗策略、全屏 |
| `src/ui/qml/components/MediaBar.qml` | 媒体控制栏；**显隐规则和排版数值的权威注释在文件顶部** |
| `src/ui/qml/components/MicaBackdrop.qml` | 自己画的云母（造一次、各处只裁） |
| `docs/待办.md` | 现在到哪儿了 / 别踩的坑 / 探针脚本清单 |

## 会咬人的约束

1. **改过任何 `.h`，必须删掉整个构建目录重配重编**。增量构建会漏依赖，
   结果是编译链接全过、一跑就崩。
2. PowerShell 长命令会被策略拦；`Remove-Item` 被挡。**写 `.ps1` 再跑**，删文件用
   `[System.IO.File]::Delete` / `[System.IO.Directory]::Delete`。
3. 提交信息**写进 `work\commitmsg.txt` 再 `git commit -F`**（长 `-m` 会被编码搞坏）。
4. **QML 里切页不能把 `MpvQmlItem` 藏起来**（render API 靠每帧渲染推进）；
   要"藏"就缩成 1×1。
5. 开关、滑块**别用 `checked: X.y` / `value: X.z` 绑属性**，一点就断。
6. 新加的 QML 文件要同时写进 `res/QmlUi.qrc`，改完要确认 rcc 真的重编了。

## 界面上的几个时间常数

| 值 | 用在哪 |
|---|---|
| 167 ms | 控制栏显隐动画（Fluent 库自己的标准） |
| 3 s | 暂停 / 图片时鼠标停手后收起 |
| 5 s | 新投上来的视频先露一会儿 |
| 250 ms | 画面单击（播放/暂停）延迟执行，等双击 |

## 下一步

控制栏右侧那排键里，**全屏**、**音量**、**显示效果**都已经是真的（音量滑块是
自己画的；显示效果是个独立窗口，十来条滑块从后端那张表长出来 —— 理由都在各自
文件里）。中间那三个键（上一首 / 播放暂停 / 下一首）也都是真的。

剩下的壳子：**字幕**（功能没做）。换曲那两键**队列里没有就灰着**，判据是
`Playback.hasNext` / `hasPrevious`。**大多数时候它们就是灰的** —— 那是如实
显示：控制点普遍不排下一条、也不按上一首（见 `DLNA实现情况.md`）。

## 配色的两条硬规矩

- **主题色实心圆底上的字形：深色那套用黑、浅色那套用白。** 库里自己的
  `FluFilledButton.qml` 就是这么定的（`textColor: FluTheme.dark ? 黑 : 白`），
  WinUI 的 `TextOnAccentFillColorPrimary` 同理。理由是**对比度**：深色那套的蓝
  是"提亮"过的（`accentColor.lighter`，和库里 `FluTheme.primaryColor` 同一档，
  实测 `#4CA0E0`），白字压上去只有 2.9:1、黑字 7.3:1。媒体栏那个播放/暂停键
  原来写死白的，2026-09-14 改成跟着这条走。
- **控制栏自己那套明暗（`darkStyle` = 压着画面 或 程序深色主题）不能拿
  `FluTheme.dark` 顶替。** 两者在"浅色主题 + 放视频"的时候会分岔 —— 栏是深色
  的，`FluTheme.dark` 却是假。凡是画在栏上的颜色（主题蓝、悬停底色、图标灰、
  字形）都得照栏自己的判据来，见 `MediaBar.qml` 里那几个 `property color`。

## 界面上的事谁来做

**点界面归他本人**（2026-09-14 定的）：这边起程序、投测试文件、读日志、抓图；
要哪一步操作就告诉他，他做完把结果或截图丢回来。脚本点界面本来就不可靠
（控制栏会自己收、浮出控件是模态的会吃掉第一下、滚轮只送给焦点窗口），
而且动的是**他正在用的那套鼠标键盘**。
